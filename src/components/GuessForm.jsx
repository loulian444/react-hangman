import { useState } from "react";

const GuessForm = () => {
  const [guessedLetter, setGuessedLetter] = useState(``);

  return (
    <>
      <h2>Guess Form</h2>
    </>
  );
};

export default GuessForm;
